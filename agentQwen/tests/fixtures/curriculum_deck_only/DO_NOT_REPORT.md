# Synthetic fixture — never report as an experiment

This directory exists only to execute and visually inspect the dynamic HTML/PPT generators before cloud artifacts arrive. Every metric is fabricated for layout coverage. Final generators still require PASS artifacts from the selected run root, and final claims must use `artifacts/cloud_curriculum/<run-id>` only.
